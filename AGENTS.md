# stevewang.me — agent guidance

Read [`CLAUDE.md`](CLAUDE.md). It is the authoritative guidance for this repository and
applies to every coding agent, whatever the file name says.

This stub exists only so agents that look for `AGENTS.md` find their way there. Do not
copy guidance into it — rules that apply to a single agent go under "Agent-specific
notes" at the bottom of `CLAUDE.md`.
